# Todo / In Progress

## In Progress
- None.

## Planned / Backlog
- Issue #40 — implement explicit PathRAG candidate discovery, scoring, and rejected-path provenance; adaptive context allocation currently consumes only its optional normalized path signal.
- Issue #36 — promote table and figure payloads into selected Full-Pipeline evidence and prompt context.
- Issue #41 — group RAPTOR-style reduction inputs by existing embedding similarity instead of fixed batches.
- Issue #42 — add a safe test/dev-only forced-fail path that proves bounded feedback retries and attempt artifacts.
- Issue #35 — add optional SummaC/FactCC adapters without making heavy evaluator models required for normal runs.
