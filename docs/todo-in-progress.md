# Todo / In Progress

## Planned / Backlog

- Execute the dual-backend ingest plan after the repo baseline is pushed: add env-selected storage/Qdrant backends, make `DoclingLoader` backend-neutral, and revive local Docker mode.

## In Progress

- Create the private GitHub repo baseline `graph-rag-summarizer` before the dual-backend execution pass.
