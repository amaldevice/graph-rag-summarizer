# Todo / In Progress

## Planned / Backlog

- Add document-safe PDF ingest modes for Qdrant collections (`append`, `replace-document`, `replace-collection`) so multi-PDF ingestion does not collide on point IDs.

## In Progress
- Fix multi-provider fallback spec gaps and shared-session wiring before shipping the current PR branch; includes issue comments, draft PR setup, regression tests, and review-driven code patches.
